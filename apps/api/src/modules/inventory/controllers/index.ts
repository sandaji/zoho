/**
 * Inventory Module - Controller Layer
 *
 * Re-export the main inventory controller implementation so the API routes
 * bind to the same controller that implements transfer request handling.
 */

export { InventoryController } from "./inventory.controller";
