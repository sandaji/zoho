/*
 * COMPATIBILITY SHIM — this file is no longer generated by Prisma.
 *
 * It used to be output of Prisma's newer `prisma-client` generator, but the
 * schema now uses `provider = "prisma-client-js"` (see prisma/schema.prisma),
 * which emits enums from ./index.js instead and never rewrites this file.
 * The old frozen copy drifted from the schema — e.g. it had no
 * SalesDocumentStatus.PARKED / HELD / CLOSED and a different TransferStatus —
 * so importing from here gave `undefined` enum members at runtime and
 * wrongly-narrow types at compile time.
 *
 * Everything below simply re-exports the LIVE enums, so any remaining
 * `.../generated/enums` import now sees the real values. New code should
 * import from "generated/index.js" (or "generated") directly; once nothing
 * imports this file any more it can be deleted.
 */

export {
  InventoryStatus,
  ProductType,
  ProductStatus,
  SalesStatus,
  SalesDocumentType,
  SalesDocumentStatus,
  PaymentStatus,
  DeliveryStatus,
  TransactionType,
  PayrollStatus,
  PaymentMethod,
  CashierSessionStatus,
  MovementType,
  TransferStatus,
  JournalType,
  FiscalStatus,
  AuditAction,
  ApprovalType,
  ApprovalStatus,
  PurchaseOrderStatus,
  AccountType,
  PeriodType,
  BudgetStatus,
  ARStatus,
  APStatus,
  BankAccountType,
  ForecastType,
  ForecastStatus,
  AlertType,
  AlertSeverity,
  AlertStatus,
  TaxType,
  TaxStatus,
  LeaveStatus,
  JobPostingStatus,
  ApplicantStatus,
  InterviewStatus,
  PerformanceStatus,
  AccessScope,
} from "./index.js";
