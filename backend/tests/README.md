# Backend Tests

To run the test suite for the backend, ensure you have all dependencies installed (`npm install`), and the server is running in another terminal (`npm run dev`).

Then, execute the following command:

```bash
npm test
```

This will run all tests, including the end-to-end tests that verify API endpoint functionality against a running server and database.
